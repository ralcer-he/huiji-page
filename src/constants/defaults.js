export const DEFAULT_WORDCLOUD_THRESHOLDS = {
  '3days': 3,
  '7days': 5,
  '30days': 20,
}

export const DEFAULT_WRITE_PAGE_TYPES = ['note', 'mood', 'memo', 'diary']

export const DEFAULT_DIARY_TOOLBAR = ['bold', 'italic', 'underline', 'strike', 'h1', 'h2', 'bulletList', 'orderedList', 'indent', 'outdent', 'image', 'speech', 'drawing', 'time', 'datetime', 'date']

export const DEFAULT_NOTE_TOOLBAR = ['bold', 'italic', 'highlight', 'image', 'drawing', 'speech']

export const DEFAULT_CALENDAR_MODE = 'simple' // 'simple' | 'detailed'

export const DEFAULT_DOMINANT_EMOTION_PERIOD = 'month' // 'week' | 'month' | 'year' | 'all'

export const DEFAULT_SHARE_CARD_CONFIG = {
  showSlogan: true,
  showWatermark: true,
  showDate: true,
  showCategory: true,
}
